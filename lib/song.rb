class Songs
  attr_accessor :name

  def initialize(name)
    @name = name
  end

  def add_song(song)
    @songs = songs.each do |song|
      @songs << self.artist.name
  end


  def add_song_by_name(song)

  end
