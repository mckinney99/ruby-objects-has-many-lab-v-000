class Songs
  attr_accessor :songs,

  def initialize
    @songs = []
    @songs
  end

  def add_song(song)
    @songs = songs.each do |song|
      self.artist.name
  end
end

  def add_song_by_name(song)

  end
